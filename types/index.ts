// Shared TypeScript types and interfaces for the portfolio.
// Import and re-export from domain-specific type files as they are created.

export type { NavItem } from "./navigation";
export type { FeaturedProject, ProjectMeta } from "./project";
// export type { Article, ArticleMeta } from './article';   — Sprint 4
